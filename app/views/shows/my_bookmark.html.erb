<h2> Bookmarked Shows </h2>
<h4> Upcoming shows (<%= @list_of_current_bookmarks.count %>)</h4>

<% if @list_of_current_bookmarks.count == 0 %>
  <p> You haven't bookmarked any current show at the moment. </p>
<% else %>
  <div class="row row-cols-1 row-cols-md-3 g-4">
    <% @list_of_current_bookmarks.each do |a_favorite| %>
      <% a_show = a_favorite.show %>
      <div class="col">
        <div class="card h-100">
          <img src="<%= a_show.image %>" class="card-img-top" style="width: 100%;" alt="<%= a_show.name %> Image">
          <div class="card-body">
            <h5 class="card-title"><%= a_show.name %></h5>
            <p class="card-text">
              <strong><%= a_show.date.strftime('%a, %b %d') %></strong><br>
              <strong><%= a_show.date.strftime('%I:%M %p') %></strong><br>
              <strong>Venue:</strong> <%= a_show.venue_name %><br>
              <strong>Address:</strong> <%= a_show.address %>, <%= a_show.city %>, <%= a_show.state %><br>
              <strong>Show Type:</strong> <%= a_show.show_type.name %>
            </p>
          </div>
          <hr>
          <div class="card-footer">
            <a href="/shows/<%= a_show.id %>" class="btn btn-primary">Show Details</a>
          </div>
        </div>
      </div>
    <% end %>
  </div>
<% end %>

<div>
  <h4> Archived shows (<%= @list_of_archived_bookmarks.count %>)</h4>
</div>

<% if @list_of_archived_bookmarks.count  == 0 %>
  <p> You do not have any archived show at the moment. </p>
<% else %>
  <div>

    <div class="row row-cols-1 row-cols-md-3 g-4">
      <% @list_of_archived_bookmarks.each do |a_favorite| %>
        <% a_show = a_favorite.show %>
        <div class="col">
          <div class="card h-100">
            <img src="<%= a_show.image %>" class="card-img-top" style="width: 100%;" alt="<%= a_show.name %> Image">
            <div class="card-body">
              <h5 class="card-title"><%= a_show.name %></h5>
              <p class="card-text">
                <strong><%= a_show.date.strftime('%a, %b %d') %></strong><br>
                <strong><%= a_show.date.strftime('%I:%M %p') %></strong><br>
                <strong>Venue:</strong> <%= a_show.venue_name %><br>
                <strong>Address:</strong> <%= a_show.address %>, <%= a_show.city %>, <%= a_show.state %><br>
                <strong>Show Type:</strong> <%= a_show.show_type.name %>
              </p>
            </div>
            <hr>
            <div class="card-footer">
              <a href="/shows/<%= a_show.id %>" class="btn btn-primary">Show Details</a>
            </div>
          </div>
        </div>
      <% end %>
    </div>

  </div>
<% end %>
